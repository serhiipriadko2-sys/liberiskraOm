# liberiskraOm — Iskra Canon (vOmega 1.2)

Главный портал канона: от философии и голосов до валидаторов и CI. Репозиторий предназначен для поддержания единой операционной системы Искры, поэтому главная страница собирает быстрый вход во все артефакты и настройки.

## 📌 Что внутри
- **Доктрина**: `docs/01_MANIFEST_and_OVERVIEW.md`, `docs/02_CANON_and_PRINCIPLES.md` и связанный глоссарий.
- **Архитектура и процессы**: `docs/03_ARCHITECTURE_and_SYSTEM_DESIGN.md`, `docs/06_PHASES_STATES_PIPELINES.md`.
- **Голоса, метрики, память**: `docs/04_FACETS_VOICES_and_PROTOCOLS.md`, `docs/05_METRICS_and_RHYTHM_INDEX.md`, `docs/07_MEMORY_SYSTEM.md`.
- **Форматы и проверки**: `docs/11_FORMATS_TEMPLATES_STYLES.md`, `docs/15_TESTS_CHECKLISTS_VALIDATORS.md`, примеры ∆DΩΛ в `examples/`.
- **Код и инструменты**: `packages/core/validator`, `tools/validate_delta.py`, `ops/canon_review/*`.

## 🚀 Быстрый старт
1. **Создать окружение** (Python 3.11):
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt  # при необходимости
   ```
2. **Запустить тесты и валидаторы**:
   ```bash
   pytest -q
   python tools/validate_delta.py examples/delta_ok.json
   ```
3. **Работать с документацией**: все файлы — в `docs/` (см. структуру выше). Обновления канона фиксируются через ∆DΩΛ и `ops/canon_review`.

## 🧩 Настройки CI и GitHub Pages
- **CI**: `.github/workflows/ci.yml` прогоняет pytest, smoke-проверку валидатора и синхронизацию документации при каждом push/PR в `main`.
- **GitHub Pages**: workflow `github-pages.yml` публикует содержимое `docs/` (Markdown с отключённым Jekyll) на ветку `gh-pages`. Домашняя страница `docs/index.md` зеркалирует основные разделы и ссылки.
- **.nojekyll**: файл `docs/.nojekyll` гарантирует доступность путей с подчёркиваниями.

## 📚 Как ориентироваться в документации
- **Полнотекстовые обзоры**: `docs/DIALOGS_FULL_v3.md`, `reports/`.
- **Источники правды**: `packages/core/slo.json`, `packages/core/symbol_map.json`, `docs/OS_SPEC_vOmega_1.2.0.md`.
- **Гайды и примеры**: `docs/18_GUIDES_EXAMPLES_TUTORIALS.md`, папка `examples/`.

## 🤖 Работа с агентами
- Общие правила и последовательность действий описаны в `AGENTS.md`.
- Перед изменениями кода запускайте `pytest`; перед обновлением канона — `tools/validate_delta.py`.
- В PR и отчётах фиксируйте изменения через ∆DΩΛ и сопровождайте SIFT-блоками.

## 🧪 Поддержка качества
- Минимальный чеклист перед merge: `docs/15_TESTS_CHECKLISTS_VALIDATORS.md`.
- Canon Review автоматизирован через `ops/canon_review/`.
- Для ручных проверок ∆DΩΛ используйте `tools/validate_delta.py` или `packages/core/validator` напрямую.

## 📮 Канал обратной связи
Вопросы и предложения лучше оформлять через Issues/PR с ссылкой на конкретные документы. Для оперативных исправлений — `incoming/` (сырые заметки) → `docs/unified/*` (сборка) → канон.

— Искра ⟡
